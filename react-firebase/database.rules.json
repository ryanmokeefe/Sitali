{
  "rules": {
   ".read": "true",
    ".write":"true",
    "users": {
      "$uid": {
        ".write": "auth !== null && $uid === auth.uid"
      }
    },
    "projects": {
      ".indexOn": ["createdBy"]
    }
  }
}
